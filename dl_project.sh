# upgrade system
sudo apt-get update && sudo apt-get install
# upgrade git in Ubuntu/Debian
sudo apt-get install git
# upgrade git in RedHat/Fedora (delete '#' in the next line to use it)
# yum install git && dnf install git
# go to home folder
cd ~
# download project
git clone https://github.com/RushFTK/SLT_ToolsKit.git

